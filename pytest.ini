[pytest]
markers =
    integration: integration tests that may require external tools
    requires_ffmpeg: requires ffmpeg
    requires_exiftool: requires exiftool
    requires_ollama: requires local Ollama server/model
    requires_internet: requires internet access
    gpu_optional: can use GPU if available
